services:
  spendee:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      SQLITE_PATH: /data/spendee.db
    volumes:
      - spendee-data:/data

volumes:
  spendee-data:
