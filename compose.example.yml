services:
  spendee:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      SQLITE_PATH: /data/spendee.db
      ACTUAL_DATA_DIR: /data/actual
    volumes:
      - spendee-data:/data

volumes:
  spendee-data:
